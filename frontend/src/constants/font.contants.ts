export const MAX_FONT_SIZE = import.meta.env.VITE_MAX_FONT_SIZE
  ? parseInt(import.meta.env.VITE_MAX_FONT_SIZE)
  : 64;
export const MIN_FONT_SIZE = import.meta.env.VITE_MIN_FONT_SIZE
  ? parseInt(import.meta.env.VITE_MIN_FONT_SIZE)
  : 12;
